<%= form_for @election, action: :create do |f| %>
  <%= f.label 'Election name' %>
  <%= f.text_field :title %>
  <label for='mails'>Emails</label>
  <input type='text' name='emails' id='mails' />
  <%= f.hidden_field :owner_id, value: current_user.id %>
  <%= f.submit %>
<% end %>

